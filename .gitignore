# dependencies
/node_modules

# misc
.DS_Store
*.pem

# local env files
.env
.env*.local

# claude setup
.claude/

# large / regenerable data (corpus splits, binary signature dumps) — not for git (exceed GitHub limits)
data/corpora/wiki*.txt
data/signatures/*.bin
